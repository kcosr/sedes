import { createHash } from "node:crypto";
import { createDarwinSidecarPlatform, DARWIN_SIDECAR_HELPER_SOURCE } from "./sidecar-darwin-platform.js";
import { createSidecarProcessOwnership } from "./sidecar-process-ownership.js";
import type { SidecarServiceScope } from "../../internal/sidecar-protocol/service-management-v1.js";

/** Fixed management carrier, independent of installed runtime artifacts. */
export const REMOTE_SERVICE_MANAGEMENT_PROXY_SOURCE = [
  "const f=require('node:fs'),p=f.promises,n=require('node:net'),o=require('node:os'),q=require('node:path'),c=require('node:crypto'),accountHome=o.userInfo().homedir",
  `const darwinOwnership=(${createDarwinSidecarPlatform.toString()})(async args=>{const h=await p.realpath(accountHome),root=q.join(h,'.local','state','sedes','sidecar','platform','darwin-'+process.arch+'-${createHash("sha256").update(DARWIN_SIDECAR_HELPER_SOURCE).digest("hex")}','ready'),helper=q.join(root,'helper'),marker=q.join(root,'checksum'),uid=process.getuid?.();for(const [name,mode] of [[root,448],[helper,320],[marker,256]]){const m=await p.lstat(name);if(m.isSymbolicLink()||m.uid!==uid||(m.mode&511)!==mode||(name===root?!m.isDirectory():!m.isFile()))throw new Error('sidecar_platform_helper_invalid')}const bytes=await p.readFile(helper),digest=await p.readFile(marker,'utf8');if(bytes.length>1048576||!/^[a-f0-9]{64}$/.test(digest)||c.createHash('sha256').update(bytes).digest('hex')!==digest)throw new Error('sidecar_platform_helper_invalid');return await new Promise((resolve,reject)=>require('node:child_process').execFile(helper,args,{timeout:5000,maxBuffer:131072,encoding:'utf8'},(error,stdout)=>error?reject(error):resolve(stdout)))})`,
  `const ownership=(${createSidecarProcessOwnership.toString()})(p,process.platform==='darwin'?darwinOwnership:undefined)`,
  "const arg=JSON.parse(Buffer.from(process.argv[1],'base64url').toString('utf8')),s=arg.scope,uid=process.getuid?.()",
  "const bad=code=>{throw new Error(code)},has=(e,x)=>e&&e.code===x",
  "const response=x=>process.stdout.write(JSON.stringify({managementVersion:1,requestId:arg.requestId,...x})+'\\n')",
  "const dir=async x=>{const z=await p.lstat(x);if(!z.isDirectory()||z.isSymbolicLink()||z.uid!==uid||(z.mode&0o077)!==0)bad('sidecar_service_namespace_invalid')}",
  "const file=async(x,max)=>{const z=await p.lstat(x);if(!z.isFile()||z.isSymbolicLink()||z.uid!==uid||(z.mode&0o077)!==0||z.size>max)bad('sidecar_service_descriptor_invalid');return JSON.parse(await p.readFile(x,'utf8'))}",
  ";(async()=>{if(!Number.isSafeInteger(uid)||uid<0||!s||Object.keys(s).sort().join(',')!=='executionEnvironmentId,installationId,principalId,tenantId'||Object.values(s).some(x=>typeof x!=='string'||!/^[A-Za-z0-9_.:-]{1,160}$/.test(x))||typeof arg.requestId!=='string'||!/^[A-Za-z0-9_.:-]{1,160}$/.test(arg.requestId))bad('sidecar_management_request_invalid');const h=await p.realpath(accountHome),key=c.createHash('sha256').update(JSON.stringify([s.installationId,s.tenantId,s.principalId,s.executionEnvironmentId])).digest('hex'),root=q.join(h,'.local','state','sedes','sidecar'),service=q.join(root,'services',key),sockdir='/tmp/sedes-'+uid+'-'+key.slice(0,24),socket=q.join(sockdir,'service.sock');const absent=async()=>{let d;try{d=await file(q.join(service,'service.json'),65536)}catch(e){if(!has(e,'ENOENT'))throw e}if(d&&ownership.validLegacyStoppedDescriptor(d)){if(Object.keys(s).some(k=>d.scope[k]!==s[k]))bad('sidecar_service_recovery_required')}else if(d){if(Object.keys(d).sort().join(',')!=='lifetime,process,scope,serviceIncarnation,state,version'||typeof d.serviceIncarnation!=='string'||d.serviceIncarnation.length<1||d.serviceIncarnation.length>160||d.version!==3||!['starting','running','stopped'].includes(d.state)||!ownership.validProcess(d.process)||!ownership.validLifetime(d.lifetime)||!ownership.lifetimeMatchesProcess(d.lifetime,d.process)||!d.scope||Object.keys(d.scope).sort().join(',')!=='executionEnvironmentId,installationId,principalId,tenantId'||Object.keys(s).some(k=>d.scope[k]!==s[k]))bad('sidecar_service_recovery_required');const retired=await ownership.lifetimeEnded(d.lifetime);if(d.state!=='stopped'&&!retired)bad(await ownership.unavailableServiceCode(d.process))}response({outcome:'absent'})};try{await dir(root);await dir(q.join(root,'services'));await dir(service)}catch(e){if(has(e,'ENOENT')){response(arg.receipt?{outcome:'receipt',receipt:null}:{outcome:'absent'});return}throw e}if(arg.receipt){if(typeof arg.receipt!=='string'||!/^[A-Za-z0-9_.:-]{1,160}$/.test(arg.receipt))bad('sidecar_management_request_invalid');const rd=q.join(service,'management-receipts'),rp=q.join(rd,c.createHash('sha256').update(arg.receipt).digest('hex')+'.json');let receipt=null;try{await dir(rd);receipt=await file(rp,4096);if(receipt.mutationId!==arg.receipt)bad('sidecar_management_receipt_identity_invalid')}catch(e){if(!has(e,'ENOENT'))throw e}response({outcome:'receipt',receipt});return}try{await dir(sockdir);const sm=await p.lstat(socket);if(!sm.isSocket()||sm.uid!==uid||(sm.mode&0o077)!==0)bad('sidecar_service_endpoint_invalid')}catch(e){if(has(e,'ENOENT')){await absent();return}throw e}const client=n.connect(socket),timer=setTimeout(()=>client.destroy(new Error('sidecar_management_connect_timeout')),5000);client.once('connect',()=>{clearTimeout(timer);process.stdin.pipe(client);client.pipe(process.stdout)});client.once('error',async e=>{clearTimeout(timer);if(has(e,'ENOENT')||has(e,'ECONNREFUSED')){try{await absent()}catch(x){response({outcome:'error',code:x.message})}}else{response({outcome:'error',code:'sidecar_management_connection_failed'})}process.stdin.destroy()});client.once('close',()=>process.stdin.destroy());process.stdout.once('error',()=>client.destroy());process.stdin.once('error',()=>client.destroy())})().catch(e=>{response({outcome:'error',code:/^[a-z][a-z0-9_]{0,119}$/.test(e?.message)?e.message:'sidecar_management_failed'});process.stdin.destroy()})",
].join(";");

export function sidecarManagementProxyCommand(input: { readonly scope: SidecarServiceScope; readonly requestId: string; readonly receipt?: string; readonly testOnlyAccountHome?: string }): string {
  const source = input.testOnlyAccountHome ? REMOTE_SERVICE_MANAGEMENT_PROXY_SOURCE.replace("o.userInfo().homedir", JSON.stringify(input.testOnlyAccountHome)) : REMOTE_SERVICE_MANAGEMENT_PROXY_SOURCE;
  const request = Buffer.from(JSON.stringify({ scope: input.scope, requestId: input.requestId, ...(input.receipt ? { receipt: input.receipt } : {}) }), "utf8").toString("base64url");
  // The audited helper contains newlines; the SSH carrier accepts one command
  // line. Encode the fixed program instead of changing its JavaScript tokens.
  const program = `eval(Buffer.from(${JSON.stringify(Buffer.from(source, "utf8").toString("base64url"))},'base64url').toString('utf8'))`;
  return `exec node -e ${quote(program)} ${quote(request)}`;
}
function quote(value: string): string { return `'${value.replaceAll("'", `'\\''`)}'`; }
